Use `help()` to print the list of available help topics.

Use `help("topic")` to print the text for a specific topic.

Example:
- `help("syntax")`
- `help("variables")`
- `help("types")`

The topic name must match one of the entries printed by `help()`.
