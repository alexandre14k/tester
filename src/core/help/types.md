The implemented value type today is `string`.

Strings are written with double quotes:
- `"hello"`
- `"world"`

When a string variable is printed by entering its label in the repl, the output keeps the string representation with quotes.

String methods currently implemented:
- `append(...)`
- `pop(...)`
